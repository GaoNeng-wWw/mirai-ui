# Button

## 基本用法

:::demo button/basic-usage
:::

## 不同大小

通过设置 `size` 参数可以设置按钮大小. 当设置 `size` 时, 圆角也会变化. 当然你也可以选择设置 `rounded` 参数来修改圆角大小

:::demo button/size
:::

## 圆角大小

:::demo button/rounded
:::

## Loading

通过传入 `loading` 来控制当前按钮加载状态.

:::demo button/loading
:::